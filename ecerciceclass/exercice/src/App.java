
public class App {
    public static void main(String[] args) throws Exception {

      Document d = new Document("of mice and men", "me", 2004);

      //  Utilisateur u = new Utilisateur("aymen", 1);
      //  u.emprunterDocument(d);

      // Etudient e = new Etudient("aymen", 1, "GI");
      // e.emprunterDocument(d);
      // e.emprunterDocument(d);
      // e.emprunterDocument(d);
      // e.emprunterDocument(d);
      // e.emprunterDocument(d);
      // e.emprunterDocument(d);
      // e.emprunterDocument(d);

      // Professeur p = new Professeur("aymen", 1, "math-info");
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);
      // p.emprunterDocument(d);




    }
}
